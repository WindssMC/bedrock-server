name: Custom Bedrock Server

on:
  workflow_dispatch:

jobs:
  run-server:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Download Playit Binary Safely
      run: |
        # Pulls the universal latest stable release to completely bypass dead link errors
        curl -L "https://github.com" -o playit
        chmod +x playit

    - name: Start Docker Server and Tunnel
      env:
        PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
      run: |
        # Launch the official Bedrock engine inside a safe background container
        docker run -d -e EULA=TRUE -p 19132:19132/udp itzg/minecraft-bedrock-server
        
        # Give the environment a few moments to spin up cleanly
        sleep 5
        
        # Initialize the network tunnel route
        if [ -z "$PLAYIT_SECRET" ]; then
          echo "===================================================="
          echo "NO PLAYIT SECRET DETECTED. CHOOSE THE LINK BELOW:"
          echo "===================================================="
          ./playit run
        else
          ./playit run --secret "$PLAYIT_SECRET"
        fi
